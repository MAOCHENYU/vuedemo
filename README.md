## Getting started

```bash
# clone the project
git clone https://github.com/MAOCHENYU/vuedemo.git

# enter the project directory
cd vuedemo

# install dependency
cnpm install

# develop
cnpm run serve
```